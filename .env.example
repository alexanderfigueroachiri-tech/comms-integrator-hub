# PostgreSQL connection (Neon, Supabase, or local Docker)
# Example local: postgresql://postgres:postgres@localhost:5432/cih_dev
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/cih_dev?schema=public"

# seed (default) | db — see ADR-0004
CIH_DATA_SOURCE=seed
