# PostgreSQL — local Docker (see docker-compose.yml) or Neon/Supabase cloud URL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cih_dev?schema=public"

# seed = demo sin DB (default) | db = leer desde PostgreSQL tras db:seed
CIH_DATA_SOURCE=seed
